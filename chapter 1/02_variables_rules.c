#include<stdio.h>

int main(){
    int a; // no wide space character allowed
    int b; // no special charcter allowed
    int pd; // no brackets allowed
    int PD_BOI; // underscore is allowed
    int _pd; // we can start with underscore
    int PD; // variables are case sensitive
}    