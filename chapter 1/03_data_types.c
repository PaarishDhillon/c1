#include <stdio.h>

int main()
{
    // int a;
    // a = 1;
    int a = 1;       // same as above
    printf("integer = %d\n", a); // %d is used for integer output
    // float b = 1.41 // use float for decimal
    float b;
    b = 1.41;
    printf("float = %f\n", b); //%f is used for float output
    char c = 'a';    // character has to be in single quote
    // char c;
    // c = a;
    printf("character = %c\n", c); // %c is used for character output
}
// (\n) is used for getting separate output of separate function